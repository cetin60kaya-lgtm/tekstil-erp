{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "ERP: Dev Up (Docker)",
      "type": "shell",
      "command": "docker compose up -d --build",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "ERP: Dev Logs (Follow)",
      "type": "shell",
      "command": "docker compose logs -f --tail=200",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": [],
      "dependsOn": ["ERP: Dev Up (Docker)"]
    },
    {
      "label": "ERP: Dev Down (Docker)",
      "type": "shell",
      "command": "docker compose down",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "ERP: Reset DB (Docker) — DANGER",
      "type": "shell",
      "command": "docker compose down -v",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    }
  ]
}
